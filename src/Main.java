void main() {
    IO.println("Hello World !");

    Employee first = new Employee("David",2000);
    IO.println(first.bonus());
}

//implement abstract class with subclasses that implement two interfaces
//call mange() subclass and interface methode
//one interfaces must have a default method call it in main()